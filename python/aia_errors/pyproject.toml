[project]
name = "aia-errors"
version = "0.1.0"
description = "Problem Details (RFC 9457) and the domain error catalogue"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aia_errors"]
