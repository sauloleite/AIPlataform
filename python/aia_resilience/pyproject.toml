[project]
name = "aia-resilience"
version = "0.1.0"
description = "Timeout, retry, circuit breaker and bulkhead"
requires-python = ">=3.12"
dependencies = ["aia-errors"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aia_resilience"]
