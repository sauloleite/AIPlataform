{
  "//": "Config de EDITOR e LINT: inclui testes e configs, que precisam de informacao de tipo para as regras tipadas rodarem. Nao emite nada.",
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "composite": false,
    "incremental": false
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "*.config.ts"],
  "exclude": ["node_modules", "dist"]
}
