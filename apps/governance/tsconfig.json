{
  "//": "EDITOR and LINT config: includes tests and configs, which need type information for the typed rules to run. It emits nothing.",
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "composite": false,
    "incremental": false
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "*.config.ts"],
  "exclude": ["node_modules", "dist"]
}
